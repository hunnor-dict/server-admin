- apt:
    name: nfs-kernel-server
    state: present
  become: true

- file:
    group: nogroup
    owner: nobody
    path: /var/opt/nfs
    state: directory
  become: true

- lineinfile:
    create: yes
    line: /var/opt/nfs *(rw,sync,no_subtree_check)
    path: /etc/exports
    state: present
  become: true

- service:
    name: nfs-kernel-server
    state: restarted
  become: true
