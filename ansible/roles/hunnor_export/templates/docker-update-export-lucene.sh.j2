#!/bin/bash

docker rm export-lucene

docker pull hunnordict/export-lucene

docker run --name export-lucene --user {{hunnor_user_id}}:{{hunnor_user_group_id}} --volume {{hunnor_directory_tools}}:/tools hunnordict/export-lucene cp /opt/export-lucene/export-lucene-1.0.0.jar /tools/
