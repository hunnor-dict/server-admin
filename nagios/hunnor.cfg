define command {
    command_name check_procs_by_name
    command_line /usr/lib/nagios/plugins/check_procs -w '$ARG1$' -c '$ARG2$' -C '$ARG3$'
}

define service {
    host_name localhost
    service_description Backup - Dropbox
    check_command check_procs_by_name!1:1!1:1!dropbox
    use service-template
    notification_interval 0
}

define service {
    host_name dict_hunnor_net
    service_description Web - HunNor dictionary
    check_command check_http!-s HunNor
    use service-template
    notification_interval 0
}

define service {
    host_name hunnor_dict
    service_description Web - HunNor dictionary
    check_command check_http!-s HunNor
    use service-template
    notification_interval 0
}

define service {
    host_name hunnor_dict_admin
    service_description Web - HunNor admin interface
    check_command check_http!-s Bejelentkezés
    use service-template
    notification_interval 0
}
